import { FileUploader } from 'react-drag-drop-files';

export default function MovieSelector({ visible }) {
    return (
        <div>
            <FileUploader></FileUploader>
        </div>
    );
}
